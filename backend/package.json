{
  "name": "cafebot-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal CafeBot backend (no framework, no dependencies).",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  }
}
