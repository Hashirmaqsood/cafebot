# Prompts

System prompts and prompt templates for CafeBot go here (e.g. `system_prompt.md`, `order_taking_prompt.md`).

Keeping prompts in their own text/markdown files (instead of hardcoding them in code) makes them easy to edit and version without touching application logic.
